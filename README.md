# TODO

Use cog packages to create package tags rather than a single repo "API" version.

Otherwise any single workflow API can't break without making other workflows
bump to a new major version that may not accurately reflect an API breakage for
them.
