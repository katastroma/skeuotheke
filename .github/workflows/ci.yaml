name: CI

on:
  workflow_dispatch:
  push:
    branches: [default]
    paths-ignore:
      - LICENSE
      - '**/*.md'

jobs:
  version:
    permissions:
      contents: write
    uses: katastroma/skeuotheke/.github/workflows/version.yaml@v0
    with:
      GH_APP_ID: ${{ vars.SKEUOTHEKE_GH_APP_CLIENT_ID }}
    secrets:
      GH_APP_PRIVATE_KEY: ${{ secrets.SKEUOTHEKE_GH_APP_PRIVATE_KEY }}
