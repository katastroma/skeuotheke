name: Generate Code from Proto Using buf
description: Generate Go code from protobuf definitions

runs:
  using: composite
  steps:
    - name: Setup mise
      uses: jdx/mise-action@v4

    - name: Generate from protos
      shell: bash
      run: buf generate
